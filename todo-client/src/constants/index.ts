// Enum for status
export enum TodoStatus {
  Pending = "pending",
  Completed = "completed",
  Incomplete = "incomplete",
}

// Enum for priority
export enum TodoPriority {
  Low = "low",
  Medium = "medium",
  High = "high",
}
